import { FormatTextDirective } from './format-text.directive';

describe('FormatTextDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatTextDirective();
    expect(directive).toBeTruthy();
  });
});
